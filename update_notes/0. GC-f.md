- Updated the links for dataset

-I encountered the UserWarning: Do not pass an input_shape/input_dim argument to a layer warning. This happens because newer versions of Keras (Keras 3 / TensorFlow 2.16+) prefer using an explicit Input object instead of passing arguments to the first layer. 

Fixed a warning (Build Network section):

Old way: model.add(Flatten(input_shape=(64, 64, 3)))

New way: model.add(Input(shape=(64, 64, 3))) followed by model.add(Flatten())

- Same idea implemented for Regularise and re-train section